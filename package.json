{
  "name": "dojo-clock",
  "scripts": {
    "test": "mocha ./**/*.test.js --ui exports --timeout 4000 --recursive --reporter spec --check-leaks"
  },
  "dependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.0.3"
  }
}
